#include<stdio.h>
int main()
{
    int n;
    scanf("%d", &n);

    for(int i=0;i<n;i++)
    {
        int n,m,rb,cb,rd,cd;
        scanf("%d %d %d %d %d %d", &n, &m, &rb, &cb, &rd, &cd);

        int count = 0;
        
    }
}